apiVersion: v1
kind: Pod
metadata:
  name: "{{ template "helloworldapi-chart.fullname" . }}-test-connection"
  labels:
    {{- include "helloworldapi-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "helloworldapi-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
